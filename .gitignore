# --- Large regenerable arrays: never commit (100s of MB each) ---
*.npy
*.npz
raw/
dask-worker-space/
.venv*/
__pycache__/
*.pyc
.DS_Store

# NOTE: network.tsv is deliberately NOT ignored -- it is the deliverable and is
# small enough to commit.  Per-strategy run_info.txt / genes.txt and the
# evaluation outputs are committed too, so results/ is reviewable on GitHub.
